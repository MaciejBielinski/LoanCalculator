# Loan Calculator
The project was based on Angular version 11.
Type `ng serve` from node.js command line to run the app.

# Description
`AppComponent` class is the main component, which contains the user interface and some basic validation.